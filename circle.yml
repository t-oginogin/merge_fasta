# Customize the test machine
machine:

  timezone:
    Asia/Tokyo

database:
  override:
    - bin/rails db:environment:set RAILS_ENV=test
    - bin/rails db:drop --trace RAILS_ENV=test
    - bin/rails db:migrate --trace RAILS_ENV=test
